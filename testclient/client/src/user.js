import React from "react";

const UserComponent = ({ CurrentUser }) => { // Renamed user to UserComponent and properly destructured CurrentUser
    return (
        <div>user</div>
    );
}

export default UserComponent;
